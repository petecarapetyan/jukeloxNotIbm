{
  "name": "walktown",
  "version": "1.0.0",
  "description": "walktown rocket source code",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rocket start",
    "build": "rocket build",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "@rocket/cli": "^0.1.4",
    "@rocket/launch": "^0.1.1"
  }
}
